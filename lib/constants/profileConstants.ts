/**
 * Profile-related constants.
 * TRA-491: Bio character limit matches profiles.bio CHECK constraint in database.
 */
export const BIO_MAX_LENGTH = 250;

/** Max length for creatable profile dropdown values (major, minor, industry). */
export const PROFILE_SELECT_FIELD_MAX_LENGTH = 200;
